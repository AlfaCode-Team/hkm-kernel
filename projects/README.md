# `projects/` — the Project Layer

The documentation for this layer lives in its own repository:
**[hkm-project-layer](https://github.com/AlfaCode-Team/hkm-project-layer)**.

This repository's documentation covers the kernel (`src/`) and the packages it
runs on (`modules/`) — and nothing else. The directory layout there mirrors this
one, so a path here maps to the same path in that repo.
